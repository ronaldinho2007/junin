echo "# junin" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/ronaldinho2007/junin.git
git push -u origin main

code opera

shutdown -a -f -260

exec notepad.exe
